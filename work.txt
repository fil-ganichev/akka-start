Ошибки:

Проверить тест с 2-мя ApiServer, когда в первом Exception
Проверить тип Void при вызове Api

Рефакторинг:

-

Доработки:

 Десериализизация ObjectApiCallArguments(с объектом-массивом) и ArrayApiCallArguments с заранее неизвестными вложенными типами. Десериализация средствами java потребует Serializable
 Почему не перехватываются нормально Exception-ы в ApiHelper? Разобраться с перехватом Exception и логированием на каждом этапе.
 Как универсально логировать каждый шаг, не ставя после него вызов log()